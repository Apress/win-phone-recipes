<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<Style x:Key="NewsTitleStyle" TargetType="TextBlock">
		<Setter Property="Foreground" Value="{StaticResource PhoneAccentBrush}" />
	</Style>
	<Style x:Key="NewsLinkStyle" TargetType="TextBlock">
		<Setter Property="Foreground" Value="White" />
		<Setter Property="FontSize" Value="13.333"/>
	</Style>
	<DataTemplate x:Key="NewsListItemStyle">
		<StackPanel>
			<TextBlock Text="{Binding Title}" Style="{StaticResource NewsTitleStyle}" />
			<TextBlock Text="{Binding Link}" Style="{StaticResource NewsLinkStyle}" />
		</StackPanel>
	</DataTemplate>
	<!-- Resource dictionary entries should be defined here. -->
</ResourceDictionary>